---
phase: quick
plan: 260413-l5j
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/main.js
  - client/src/styles.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "A single tool selector button shows the currently active tool"
    - "Clicking the button opens a dropdown with Cursor, Paint, and Region Grow options"
    - "Selecting an option closes the dropdown and activates that tool"
    - "The button reflects the active tool name/icon at all times"
  artifacts:
    - path: "client/src/main.js"
      provides: "Tool dropdown replacing three separate tool buttons"
    - path: "client/src/styles.css"
      provides: "Dropdown positioning and styling"
  key_links:
    - from: "dropdown option click"
      to: "state.setActiveTool()"
      via: "data-tool attribute"
    - from: "state.activeTool"
      to: "dropdown button label"
      via: "state.subscribe(updateActiveTool)"
---

<objective>
Replace the three separate tool buttons (crosshair ⌖, Paint 🖌, Grow2D) with a single dropdown button that shows the active tool and expands to reveal all options.

Purpose: Reduces toolbar clutter — three buttons become one compact selector.
Output: Single compact dropdown button in the tool section of main.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace tool buttons with a dropdown in main.js</name>
  <files>client/src/main.js</files>
  <action>
In the tool selection section (around line 544-577):

1. Replace the `toolSec.innerHTML` that renders three `<button class="tool-btn compact-btn">` elements with a single dropdown button structure:

```html
<div id="tool-dropdown" style="position:relative;flex:1;">
  <button id="tool-dropdown-btn" class="compact-btn" style="width:100%;display:flex;align-items:center;justify-content:space-between;gap:4px;">
    <span id="tool-dropdown-label">⌖ Cursor</span>
    <span style="font-size:10px;">▾</span>
  </button>
  <div id="tool-dropdown-menu" style="display:none;position:absolute;top:100%;left:0;right:0;z-index:100;background:#fff;border:1px solid #ccc;border-radius:4px;box-shadow:0 2px 8px rgba(0,0,0,0.15);margin-top:2px;">
    <button class="tool-option compact-btn" data-tool="crosshair" data-label="⌖ Cursor" style="width:100%;text-align:left;border:none;border-radius:0;flex:unset;">⌖ Cursor</button>
    <button class="tool-option compact-btn" data-tool="paint" data-label="🖌 Paint" style="width:100%;text-align:left;border:none;border-radius:0;flex:unset;">🖌 Paint</button>
    <button class="tool-option compact-btn" data-tool="region-grow" data-label="⬡ Grow2D" style="width:100%;text-align:left;border:none;border-radius:0;flex:unset;">⬡ Grow2D</button>
  </div>
</div>
```

2. Replace the `toolBtns` / click listener block with:
   - Get refs: `toolDropdownBtn`, `toolDropdownMenu`, `toolDropdownLabel` from the DOM.
   - Toggle dropdown open/close on `toolDropdownBtn` click.
   - Close dropdown on outside click (`document.addEventListener('click', handler)`; check `!toolSec.contains(e.target)`).
   - For each `.tool-option`: on click, call `state.setActiveTool(btn.getAttribute('data-tool'))` and close the menu.

3. Replace `updateActiveTool` to update the label button text:
   - Find the option whose `data-tool` matches `state.activeTool`.
   - Set `toolDropdownLabel.textContent` to that option's `data-label`.
   - Highlight active option in menu: matching option gets `background:#e8f0fe; color:#4a9eff;`, others reset to `background:''`.

4. Keep `state.subscribe(updateActiveTool)` and the initial `updateActiveTool()` call as before.

Do NOT remove the `toolSec` div itself — just replace its contents. The `toolSec` still serves as the container and its existing flex/gap styles remain correct.
  </action>
  <verify>
    <automated>cd /Users/bje/repos/NextEd/client && npx vite build --mode development 2>&1 | tail -5</automated>
  </verify>
  <done>Build succeeds with no errors. The tool section renders a single dropdown button that shows the active tool name and expands to a list of three options on click.</done>
</task>

<task type="auto">
  <name>Task 2: Add dropdown menu CSS to styles.css</name>
  <files>client/src/styles.css</files>
  <action>
Append styles for the tool dropdown menu items at the end of the existing `.compact-btn` block area (after line ~577):

```css
.tool-option:hover {
  background: #f0f4ff;
}

.tool-option:first-child {
  border-radius: 4px 4px 0 0;
}

.tool-option:last-child {
  border-radius: 0 0 4px 4px;
}
```

These ensure hover feedback and correct border-radius on the first/last menu items. No other style changes needed — the dropdown uses inline styles for positioning so it works without existing layout assumptions.
  </action>
  <verify>
    <automated>grep -n "tool-option" /Users/bje/repos/NextEd/client/src/styles.css</automated>
  </verify>
  <done>styles.css contains `.tool-option:hover` rule.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Single tool dropdown replacing the three separate tool buttons. Shows active tool label, expands on click, closes on outside click, activates the selected tool.</what-built>
  <how-to-verify>
    1. Run `cd /Users/bje/repos/NextEd && uvicorn server.main:app --reload &amp; cd client &amp;&amp; npm run dev`
    2. Open http://localhost:5173 and load a volume.
    3. Confirm the tool section shows a single button (e.g. "⌖ Cursor ▾").
    4. Click it — dropdown opens with Cursor, Paint, Grow2D options.
    5. Click "🖌 Paint" — dropdown closes, button label updates to "🖌 Paint", paint tool is active.
    6. Click "⬡ Grow2D" — dropdown closes, button label updates to "⬡ Grow2D", region-grow tool is active.
    7. Click outside the dropdown — it closes without changing the tool.
    8. Confirm the Region Grow settings panel and Paint constraints panel still toggle correctly based on active tool.
  </how-to-verify>
  <resume-signal>Type "approved" if the dropdown works correctly, or describe any issues.</resume-signal>
</task>

</tasks>

<verification>
Build passes (vite build --mode development exits 0). Dropdown toggles correctly. Active tool state drives both the button label and downstream panel visibility (rgSec / constrSec via updateToolPlanes which is already subscribed to state).
</verification>

<success_criteria>
Three tool buttons are gone. One compact dropdown button exists in their place. Selecting a tool from the dropdown sets state.activeTool correctly, updates the button label, and shows the correct settings panel below.
</success_criteria>

<output>
After completion, create `.planning/quick/260413-l5j-combine-the-buttons-for-cursor-paint-reg/260413-l5j-SUMMARY.md` with what was done, files changed, and any notable implementation decisions.
</output>
