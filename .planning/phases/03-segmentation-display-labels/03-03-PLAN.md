---
phase: 03-segmentation-display-labels
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - server/catalog/models.py
  - server/main.py
  - client/src/viewer/labelManager.js
  - server/tests/test_seg_discovery.py
  - client/src/__tests__/labelManager.test.js
autonomous: true
requirements: [SEGD-05]
must_haves:
  truths:
    - "Server discovers *_segmentation.json alongside *_segmentation.nii.gz if it exists"
    - "Segmentation API includes pre-defined label name/color arrays when companion JSON present"
    - "Frontend labelManager merges pre-defined labels overriding auto-discovered default palette names"
  artifacts:
    - path: "server/catalog/models.py"
      provides: "SegmentationMetadata schema with optional custom label list"
    - path: "server/main.py"
      provides: "JSON parsing of sibling metadata files during discovery"
    - path: "client/src/viewer/labelManager.js"
      provides: "Logic to prefer API metadata colors/names over dummy auto-generated entries"
---

<objective>
Fix UAT Gap: Implement companion JSON metadata support to allow semantic label naming and reliable colors for segmentations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-segmentation-display-labels/03-UAT.md
@.planning/debug/segmentation-companion-json.md

During UAT, the user flagged that auto-discovering label semantic names (e.g. "Label 1") isn't sufficient since NIfTI lacks modality and text metadata. We need to optionally discover and read a companion `.json` file that provides the `labels` array (defining `value`, `name`, and optionally `color` as hex strings) alongside the `.nii.gz` file.
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Server-side JSON discovery and models</name>
  <files>server/catalog/models.py, server/main.py, server/tests/test_seg_discovery.py</files>
  <behavior>
    - SegmentationMetadata model includes an optional `labels` field (`list[dict] = Field(default_factory=list)`).
    - `_find_companion_segmentations` looks for a JSON file matching the exact prefix of the discovered `.nii.gz` file (e.g., `image_segmentation.json` for `image_segmentation.nii.gz`).
    - If found, it parses the JSON and populates the `labels` field.
  </behavior>
  <action>
    1. Update `server/catalog/models.py` `SegmentationMetadata` to add `labels` holding `value`, `name`, and `color`.
    2. Update `server/main.py` inside `_find_companion_segmentations` to check if `f"{prefix}.json"` exists. If true, load it JSON, extract `labels`, and apply to `SegmentationMetadata`.
    3. Update `server/tests/test_seg_discovery.py` to mock `Path.exists` returning true for JSON metadata and verify it attaches attributes.
  </action>
  <verify>
    <automated>cd /Users/bje/repos/NextEd/server && uv run pytest tests/test_seg_discovery.py -v</automated>
  </verify>
  <acceptance_criteria>
    - SegmentationMetadata has `labels` field.
    - JSON discovery logic parses sibling .json.
    - Tests for discovery logic pass.
  </acceptance_criteria>
  <done>Server loads companion semantic metadata and serves it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Frontend label priority logic</name>
  <files>client/src/viewer/labelManager.js, client/src/__tests__/labelManager.test.js, client/src/viewer/ViewerState.js</files>
  <behavior>
    - `discoverLabels(segVolume, metadataLabels)` accepts server-provided label arrays.
    - It maps the discovered integer back to the metadata; if defined, it uses the provided `name` and parses the `color` (hex).
    - If no metadata exists for a detected integer, it falls back to the old dummy "Label X" behavior.
  </behavior>
  <action>
    1. Update `discoverLabels` in `client/src/viewer/labelManager.js` to accept a second parameter `apiLabels = []`.
    2. Import `hexToRgb` from `colorPalette.js` inside `labelManager.js`.
    3. During the `for (const val of sorted)` loop, check if `apiLabels` has an entry with `value === val`. If so, use its `name` and `hexToRgb(color)`. If not, use defaults.
    4. Update `client/src/viewer/ViewerState.js` `setSegmentation(segVolume, segDims, apiLabels)` to pass the optional API labels down to `discoverLabels`.
    5. Add unit tests for `discoverLabels` passing a metadata override array.
  </action>
  <verify>
    <automated>cd /Users/bje/repos/NextEd/client && npx vitest run src/__tests__/labelManager.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `discoverLabels` correctly maps explicit metadata onto voxel values.
    - Hex colors are parsed to rgb.
    - Fallback logic still works for omitted integers.
    - Tests pass.
  </acceptance_criteria>
  <done>Frontend prioritizes companion metadata over auto-generation.</done>
</task>
</tasks>

<verification>
- `uv run pytest server/tests/` passes
- `npx vitest run` passes
</verification>

<success_criteria>
- Semantic labeling JSON overriding works full stack.
</success_criteria>

<output>
Create .planning/phases/03-segmentation-display-labels/03-03-SUMMARY.md
</output>
